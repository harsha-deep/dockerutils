# dockerutils
A lightweight desktop tool to automate docker deployments with real time logs.

```
  bun install
```

For Desktop development, run:
```
  bun run tauri dev
```

Build all installers (main command):
```
  bun run tauri build 
```